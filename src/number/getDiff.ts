/**
 * Get difference of two values.
 *
 * @deprecated Just do the diference youself. It's not that hard.
 *
 * @param a
 * @param b
 *
 * @returns the difference of `a` and `b`.
 */
export const getDiff = (a: number, b: number): number => a - b
