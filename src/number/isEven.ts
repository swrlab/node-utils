/**
 * Check if a number is even.
 *
 * @deprecated use a plain `value % 2 === 0` yourself.
 *
 * @param value - input number
 * @returns `true` if even; `false` otherwise.
 */
export const isEven = (value: number): boolean => value % 2 === 0
