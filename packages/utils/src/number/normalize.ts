/**
 * Normalize value by division of a maximum value.
 *
 * @deprecated just use a plain division.
 *
 * @param value
 *
 * @param maximumValue
 *
 * @returns normalized value.
 */
export const normalize = (value: number, maximumValue: number): number => value / maximumValue
