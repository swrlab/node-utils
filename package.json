{
	"name": "node-utils-workspace",
	"version": "3.0.0-beta.3",
	"private": true,
	"packageManager": "bun@1.2.22",
	"description": "SWR Audio Lab utilities collection for Node.js.",
	"type": "module",
	"license": "ISC",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/swrlab/node-utils.git"
	},
	"author": "SWR Audio Lab <lab@swr.de>",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"ard:coreId": "bun ./packages/utils/src/ard/coreId.ts",
		"attw": "attw --pack . --profile esm-only",
		"biome": "biome check",
		"build": "bun run --filter './packages/{utils,storage,legacy}' build",
		"check": "bun run --parallel lint format:check typecheck",
		"format": "prettier --write .",
		"format:check": "prettier --check .",
		"format:check:beta": "prettier --experimental-cli --config-path ./prettier.config.js --check .",
		"knip": "knip",
		"lint": "eslint",
		"lint:fix": "eslint --fix",
		"release": "bumpp",
		"test": "bun run --filter './packages/{utils,storage,legacy}' test",
		"test:bun": "bun run --filter './packages/utils' test:bun",
		"test:node": "bun run --filter '*' test:node",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"biome": "^0.3.3"
	},
	"devDependencies": {
		"@arethetypeswrong/cli": "^0.18.2",
		"@biomejs/biome": "2.4.4",
		"@swrlab/style-guide": "^2.1.6",
		"@types/node": "^25.3.0",
		"bumpp": "^10.4.1",
		"eslint": "^10.0.2",
		"knip": "^5.85.0",
		"prettier": "^3.8.1",
		"typescript": "^5.9.3",
		"vitest": "^4.0.18"
	},
	"devEngines": {
		"runtime": {
			"name": "node",
			"version": "^22.18.0 || >= 24.6",
			"onFail": "warn"
		},
		"packageManager": {
			"name": "bun",
			"onFail": "warn"
		}
	},
	"engines": {
		"node": "^22.18.0 || >= 24.6.0"
	}
}
